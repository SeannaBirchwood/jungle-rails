<!DOCTYPE html>
<html>

	<head>
		<meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
	</head>

	<body>
		<h4>Dearest Patron,</h4>
		<br>
		<br>
		<br>
		<div>
			<p>Thank you for bequeathing your fine dollars to us for this very eccentric and sundry list of items. Please take note of the important information below, and never act upon it.
			</p>
			<br>
			<p>If you wish to make a return, please find someone who cares and tell them about it.</p>
		</div>
		<br>
		<br>
		<br>
		<div>
			<p>Postscript</p>
			<p>We at Jungle appreciate your dollars</p>
		</div>
<% byebug %>
		<tbody>
      <% @line_items.each do |line_item| %>
        <tr>
          <td>
        <h4><%= line_item.product.name %></h4>
       <br>
       <p><%= line_item.product.description %></p>
        </td>
       <td>
        <%= number_to_currency(line_item.product.price_cents / 100) %>
        </td>
        <td>
        <%= line_item.quantity %>
        </td>
        <td>
        <% total = number_to_currency(line_item.item_price_cents / 100 * line_item.quantity) %>
        <%= total %>
        </td>
      <% end %>
      </tbody>
      <tfoot>
        <tr>
          <th>
            TOTAL:
          </th>
          <th>
       <% total_sales = @line_items.map do |item| %>
        <% item.item_price_cents / 100 * item.quantity %>
        <% end %>
        <%= number_to_currency(total_sales.sum) %>
          </th>
        </tr>
</tfoot>
	</body>
</html>